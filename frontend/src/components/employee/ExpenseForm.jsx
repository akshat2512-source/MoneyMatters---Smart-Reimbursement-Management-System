// Fields: amount, currency (dropdown via country API), category, description, date
// Shows converted amount in company currency. Has OCRUpload trigger.
export default function ExpenseForm() { return null; }
